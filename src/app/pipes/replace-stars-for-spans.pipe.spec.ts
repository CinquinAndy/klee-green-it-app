import { ReplaceStarsForSpansPipe } from './replace-stars-for-spans.pipe';

describe('ReplaceStarsForSpansPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceStarsForSpansPipe();
    expect(pipe).toBeTruthy();
  });
});
