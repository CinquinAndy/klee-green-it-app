import { FormatDatetimelocalPipe } from './format-datetimelocal.pipe';

describe('FormatDatetimelocalPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDatetimelocalPipe();
    expect(pipe).toBeTruthy();
  });
});
